'use strict';

/**
 * month-selector controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::month-selector.month-selector');
